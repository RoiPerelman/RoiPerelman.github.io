import React from 'react';
import { Header } from 'components/Header';

export const App: React.FC = () => {
  return (
    <>
      <Header />
      <section>
        <div style={{ height: 600 }}>
          this is where the markdown will go
        </div>
        <div style={{ height: 600 }}>
          this is where the markdown will go
        </div>
        <div style={{ height: 600 }}>
          this is where the markdown will go
        </div>
        <div style={{ height: 600 }}>
          this is where the markdown will go
        </div>
        <div style={{ height: 600 }}>
          this is where the markdown will go
        </div>
        <div style={{ height: 600 }}>
          this is where the markdown will go
        </div>
      </section>
    </>
  );
}
